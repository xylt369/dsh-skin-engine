# dsh-skin-engine 🎨

给 DeepSeek Harness（dsh）Web UI 换皮肤的客户端插件。

## 部署

在终端执行：

```bash
dsh plugin --profile web add @yeesy369/dsh-skin-engine
```

然后重启 dsh：

```bash
dsh web
```

打开页面后，点击侧边栏底部的「🎨 换肤中心」即可使用。
